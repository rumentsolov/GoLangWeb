{{template "base"}}

{{define "content"}}
<div class="container">
    <div class="row">
        <div class="col">
         <h1>This is the about page</h1>   
         <p>This is paragraph of text tested</p>

         <p>Passed from the GoLang to the template : {{index .StringMap "test"}}</p>
         <p>
            {{if ne (index .StringMap "remote_ip") ""}}
                Your remote IP address is {{index .StringMap "remote_ip"}}
            {{else}}
                I dont know your IP address yet. Goto <a href="/">Home page</a> so i can set it
            {{end}}
         </p>
        <div>
    <div>
</div>
{{end}}